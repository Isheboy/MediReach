# Copy to .env and fill values
# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/medireach

# JWT secret (generate a strong random value)
JWT_SECRET=replace_with_a_strong_secret

# Africa's Talking or SMS provider settings (if used)
AT_USERNAME=sandbox
AT_API_KEY=your_at_api_key
AT_FROM=MEDIREACH

# SMS provider to use: africastalking | twilio | mock
SMS_PROVIDER=africastalking

# Server port
PORT=5000
# Server
PORT=4000
NODE_ENV=development

# Database
MONGO_URI=mongodb://localhost:27017/medireach

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_in_production

# SMS Provider: 'mock' | 'africastalking' | 'twilio'
SMS_PROVIDER=mock

# Africa's Talking
AT_USERNAME=sandbox
AT_API_KEY=your_api_key
AT_FROM=your_shortcode

# Twilio
TWILIO_ACCOUNT_SID=your_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_FROM=+1234567890

# Scheduler
REMINDER_POLL_INTERVAL_SECONDS=60

# Timezone
TZ=Africa/Dar_es_Salaam
