# ==========================================
# MediReach Backend Environment Configuration
# ==========================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ==========================================
# SERVER CONFIGURATION
# ==========================================
PORT=5000
NODE_ENV=development

# ==========================================
# DATABASE (REQUIRED)
# ==========================================
# For local development with MongoDB Compass:
# MONGO_URI=mongodb://localhost:27017/medireach

# For production with MongoDB Atlas:
# 1. Create a free cluster at https://cloud.mongodb.com
# 2. Create a database user with read/write permissions
# 3. Whitelist your IP (or 0.0.0.0/0 for Render/cloud hosting)
# 4. Get your connection string and replace <username>, <password>, <cluster>
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/medireach?retryWrites=true&w=majority

# ==========================================
# JWT AUTHENTICATION (REQUIRED)
# ==========================================
# Generate a secure random string (run this command):
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_64_character_random_string_here

# ==========================================
# FRONTEND URL (REQUIRED for CORS)
# ==========================================
# Comma-separated list of allowed origins
# Development:
FRONTEND_URL=http://localhost:3000
# Production (add your deployed frontend URL):
# FRONTEND_URL=http://localhost:3000,https://your-app.vercel.app

# ==========================================
# SMS PROVIDER CONFIGURATION
# ==========================================
# Choose one: 'mock' | 'africastalking' | 'twilio'
SMS_PROVIDER=mock

# ------------------------------------------
# Africa's Talking (Recommended for Tanzania)
# ------------------------------------------
# Sign up at: https://africastalking.com
AT_USERNAME=sandbox
AT_API_KEY=your_africastalking_api_key
AT_FROM=MEDIREACH

# ------------------------------------------
# Twilio (International SMS)
# ------------------------------------------
# Sign up at: https://www.twilio.com
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM=+1234567890
TWILIO_MESSAGING_SERVICE_SID=MGxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ==========================================
# SCHEDULER CONFIGURATION
# ==========================================
# How often to check for reminders to send (in seconds)
REMINDER_POLL_INTERVAL_SECONDS=60

# ==========================================
# TIMEZONE
# ==========================================
# Set to your application's timezone
TZ=Africa/Dar_es_Salaam
